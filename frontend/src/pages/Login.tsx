import{useState}from'react';import{ShieldCheck}from'lucide-react';import{useAuth}from'../auth'
export default function Login(){const{login}=useAuth();const[id,setId]=useState('PROD001');const[pw,setPw]=useState('Demo123!');const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(id,pw)}catch(e){setError((e as Error).message)}finally{setBusy(false)}}return <main className="grid min-h-screen place-items-center bg-[#172033] p-4"><div className="w-full max-w-md"><div className="mb-6 text-center text-white"><div className="mx-auto mb-3 grid h-16 w-16 place-items-center rounded-2xl bg-amber-500"><ShieldCheck size={34}/></div><h1 className="text-2xl font-black">Permis de Travail</h1><p className="mt-1 text-slate-300" dir="rtl">ترخيص عمل — عجيلغاز رادس</p></div><form onSubmit={submit} className="card p-6"><label className="label">Matricule / Email</label><input className="input" value={id} onChange={e=>setId(e.target.value)} autoComplete="username"/><label className="label mt-4">Mot de passe</label><input className="input" type="password" value={pw} onChange={e=>setPw(e.target.value)} autoComplete="current-password"/>{error&&<p className="mt-3 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={busy} className="btn btn-primary mt-5 w-full">{busy?'Connexion…':'Se connecter'}</button><div className="mt-5 rounded-xl bg-slate-50 p-3 text-xs text-slate-600"><b>Comptes de démonstration</b><div className="mt-2 grid grid-cols-2 gap-2"><button type="button" className="btn btn-outline min-h-9 p-2" onClick={()=>setId('PROD001')}>Production</button><button type="button" className="btn btn-outline min-h-9 p-2" onClick={()=>setId('HSE001')}>HSE</button><button type="button" className="btn btn-outline min-h-9 p-2" onClick={()=>setId('PREST001')}>Prestataire</button><button type="button" className="btn btn-outline min-h-9 p-2" onClick={()=>setId('ADMIN001')}>Admin</button></div><p className="mt-2">Mot de passe : Demo123!</p></div></form></div></main>}

